<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="5dp"
    android:paddingRight="5dp"
    android:paddingTop="5dp"
    android:paddingBottom="5dp" tools:context=".MainActivity">

    <Button
        android:layout_width="300dp"
        android:layout_height="40dp"
        android:text="use background thread"
        android:layout_marginTop="50dp"
        android:onClick="onEngineRunOnBackground"/>
    <Button
        android:layout_width="300dp"
        android:layout_height="40dp"
        android:text="run new rendering engine"
        android:layout_marginTop="90dp"
        android:onClick="onEngineRun"/>
    <Button
        android:layout_width="300dp"
        android:layout_height="40dp"
        android:text="render bitmap one by one"
        android:layout_marginTop="130dp"
        android:onClick="onEngineRunOnBackgroundOneByOne"/>

    <LinearLayout
        android:orientation="vertical"
        android:id="@+id/showing_area"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"></LinearLayout>
</RelativeLayout>
